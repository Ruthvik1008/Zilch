class History{
  String title;
  String subText;
  String url;
  String expirationInfo;
  String expirationDate;

  History({this.title, this.subText, this.url,this.expirationInfo,this.expirationDate});
}